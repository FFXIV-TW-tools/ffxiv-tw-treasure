# ffxiv-tw-treasure R2 修復計畫（2026-07-11）

> 依據：同目錄 `-health-review.md`。零須修改——全建議批。基線 54 assert 只准升；push/deploy＝STOP。

## 批次 1：worker 防護對齊（1 commit ⚠ 動 worker src → 需 deploy）
- sec A2 POST /room 補 `originAllowed`（比照 WS 路徑 L192）
- sec A1 WS 升級加單房連線軟上限（`getWebSockets().length >= 32` 拒 503）
- quality A2 seed 65 點 `!validateState` 斷言一行
- 測試：worker.test 補 origin 拒絕＋連線上限案例

## 批次 2：前端 UX/a11y（1-2 commit）
- U1 卡片常駐 ➕（已加入切 ✓）＋route-empty 文案對齊「點卡片即可加入」
- U2 showStep 移焦（面板標題 tabindex=-1＋focus）
- U3 斷線/重連/opError 同步 announce() 進 #tre-status
- U4 route-stat 顯示「已完成 X / 共 Y 點」
- U5 route-panel 初始 hidden

## 批次 3：測試/文件收尾（1 commit）
- quality A1 drift 死 CSS 改 token 邊界正則（改完故意刪一個父類引用驗證會抓到，再還原）
- A3 room.js backoff 計算＋join code 淨化抽純函式＋測試
- docs A1 刪「（部署後）」／A2 檔案大小自述據實化

## 執行備註
逐主題 commit 繁中、不加 Co-Authored-By、不 push；批次 1 動 worker → 回報列 `pnpm -C worker cf:deploy:dry` 驗證＋`cf:deploy`（STOP by shawn）；B-001~003 不動。
